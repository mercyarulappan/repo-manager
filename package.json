{
  "name": "repo-manager",
  "version": "1.0.0",
  "description": "Sign in with GitHub, then switch repo visibility or delete repos from one page.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0"
  }
}
